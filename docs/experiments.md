# 실험 기록

각 라운드는 **데이터 구성 / 촬영 방식 / 결과 / 실패 원인 가설 / 다음 라운드 조치** 로 기록합니다.
태스크는 전 라운드 동일: `Pick up the blue cube and place it in the paper cup.`

평가 방식과 실패 유형 정의는 [protocol.md](protocol.md) 참고.

> **데이터셋 이름 주의** — 이름의 숫자가 실험 순서와 다릅니다.
>
> | 라운드 | 데이터셋 | 정책 |
> | --- | --- | --- |
> | 1차 | `k-chan-l/pick_and_place2` | `k-chan-l/pick_and_place2_act` |
> | 2차 | `k-chan-l/pick_and_place` (숫자 없음) | `k-chan-l/pick_and_place_act` |
> | 3차 | `k-chan-l/pick_and_place3` | `k-chan-l/pick_and_place3_act` |
> | 4차 | `k-chan-l/pick_and_place4` (3차+교정 병합) | `k-chan-l/pick_and_place4_act` |
>
> 별도로 HF에 있는 `k-chan-l/pick_and_place_20260826_171212`(10 에피소드, 1차보다 앞선 날짜)는 파이프라인 확인용 예비 촬영으로 보이며 학습에 사용하지 않았습니다. `k-chan-l/lekiwi_pick_and_place`는 다른 로봇(LeKiwi) 데이터로 이 실험과 무관합니다.

---

## 단계 구분

| 단계 | 라운드 | 성격 |
| --- | --- | --- |
| **탐색** | 1~4차 (4b, A/B 포함) — 종료 | 한 라운드에 변수를 여러 개 바꿔가며 병목을 좁힌 구간. 라운드 간 직접 비교가 제한적입니다. |
| **통제 실험** | 5차~ | 촬영·평가 조건을 고정하고 한 번에 한 변수만 바꿉니다. → [protocol.md](protocol.md) |

---

# 탐색 단계 (1~4차)

## 1차 — 정리되지 않은 환경에서의 베이스라인

### 데이터 구성
- 데이터셋: [`k-chan-l/pick_and_place2`](https://huggingface.co/datasets/k-chan-l/pick_and_place2)
- **50 에피소드 / 17,480 프레임 (30fps, 평균 약 11.7초/에피소드)**
- 성공 시연만 수집. **정지(no-op) 데이터 없음.**

### 촬영 방식
- 책상 위에 큐브·컵 외에 잡동사니(공구, 케이블, 문서 등)가 그대로 남아 있는 상태에서 촬영.
- `cam_top` / `cam_wrist` 2뷰, leader 팔로 teleoperation.
- 시연자는 카메라 화면이 아니라 실제 로봇팔을 직접 보면서 조작.

### 학습
- 정책: [`k-chan-l/pick_and_place2_act`](https://huggingface.co/k-chan-l/pick_and_place2_act)
- ACT / ResNet-18 / chunk_size 100 / **batch_size 8** / lr 1e-5 / 100,000 steps / save_freq 10,000
- `image_transforms.enable=false` (이미지 증강 없음)
- 학습 환경: 로봇 제어 PC 로컬 (RTX 3070 Laptop, 8GB)

### 결과
- **성공률 0% — 전 시도 실패.**
- 대표 실패 양상: 큐브가 있는 위치와 무관하게 팔이 특정 궤적을 재생하고 **허공에서 그리퍼를 닫음**(허공 파지). 즉 관측을 보고 목표에 접근하는 것이 아니라 시연 궤적의 평균을 그대로 흉내내는 거동.

### 실패 원인 가설
1. **배경 노이즈** — 잡동사니 때문에 이미지에서 큐브가 차지하는 시각적 비중이 작고, 정책이 큐브 위치라는 실제 원인 변수 대신 배경/궤적 타이밍 같은 상관 변수에 붙어버림 (causal confusion).
2. **표본 부족** — 큐브 위치 분포를 충분히 커버하지 못해 위치 조건부 정책이 아니라 단일 평균 궤적으로 수렴. *다만 에피소드 수 자체는 2·3차와 같은 50개이므로, 부족했던 것은 절대 개수보다 **큐브 위치의 다양성** 쪽일 가능성이 큽니다.*
3. 정지 상태 데이터가 전혀 없어, "지금은 움직이지 않아야 한다"는 행동 자체를 학습할 기회가 없었음.

### 다음 라운드 조치
- 책상을 정리하고 **타겟 물체(큐브, 컵)만** 남긴 상태로 재촬영.
- **정지 데이터 추가** — 다음 3종 상황에서 팔이 움직이지 않는 에피소드를 수집:
  - 컵만 있고 큐브 없음
  - 큐브 없음 (빈 작업공간)
  - 큐브가 이미 컵 안에 들어가 있음 (태스크 완료 상태)

---

## 2차 — 환경 정리 + 정지 데이터 도입

### 데이터 구성
- 데이터셋: [`k-chan-l/pick_and_place`](https://huggingface.co/datasets/k-chan-l/pick_and_place) ← 숫자 없는 쪽이 2차
- **50 에피소드 / 14,315 프레임 (평균 약 9.5초/에피소드)** — 그중 **정지 데이터 20개 (40%)**, 성공 시연 30개.
- 1차보다 총 프레임이 오히려 줄어든 것은 짧은 정지 에피소드가 40%를 차지하기 때문입니다.
- 정지 데이터는 1차 조치대로 3종 상황(컵만 / 큐브 없음 / 큐브가 이미 컵 안)으로 구성.

### 촬영 방식
- 책상 정리 완료, 큐브와 종이컵만 배치.
- `cam_top` / `cam_wrist` 2뷰, 640×480 / 30fps.
- **시연자가 여전히 카메라 화면이 아닌 실제 로봇팔을 직접 보면서 조작.**
- 에피소드 길이 30초 (`--dataset.episode_time_s=30`), 리셋 5초.

### 학습
- 정책: [`k-chan-l/pick_and_place_act`](https://huggingface.co/k-chan-l/pick_and_place_act)
- ACT / ResNet-18 / chunk_size 100 / **batch_size 16** (1차 8에서 증가) / lr 1e-5 / 100,000 steps / save_freq 5,000
- `image_transforms.enable=false` (이미지 증강 없음)
- 학습 환경: **로봇 제어 PC가 아닌 별도 랩탑** (RTX 4070 추정 — 확인 필요). 1차보다 VRAM 여유가 생겨 batch_size를 8→16으로 올릴 수 있었던 것으로 보입니다.
- 출력 경로가 `outputs/train/act_so101/pick_and_place_b16` 로, 배치 크기를 바꿔가며 비교한 흔적이 남아 있습니다.

### 결과
- **10회 평가 중 2회 성공 (20%)**, 30초 제한.
- 단계별 관찰:
  - **접근**: 큐브 방향으로 근접하는 것까지는 대체로 성공 — 1차의 "허공 파지"는 사라짐.
  - **파지**: 정밀 파지에서 실패가 집중. 근접은 하나 마지막 정렬/그립 타이밍이 어긋나 큐브를 놓침 (근접 오차 / 파지 실패).
  - **파지 후**: 일단 큐브를 잡기만 하면 컵으로 이동해 넣고 원위치 복귀까지 **정상 동작**.
  - **정지**: 큐브가 없으면 움직이지 않음 — 정지 데이터가 의도대로 학습됨.
- 요약: 병목은 **파지 직전 정밀 정렬 단계** 하나로 좁혀짐.

### 실패 원인 가설
1. **(a) 관측-시연 불일치** — 시연자가 카메라가 아닌 실물을 보고 조작했기 때문에, 사람은 깊이/시차/미세 정렬을 눈으로 보정하면서 움직였지만 **그 정보는 정책의 관측(2개 카메라 이미지)에 존재하지 않음**. 정책 입력만으로는 재현 불가능한 시연을 학습시킨 셈이라, 정확한 파지에 필요한 단서가 데이터에 없다.
2. **(b) 이미지-액션 시간 정렬 손상** — 카메라 스트림에 프레임 드랍/지연이 있으면 기록된 이미지와 같은 타임스텝의 액션이 실제로는 다른 시점의 장면에 대응하게 됨. 접근처럼 오차 허용범위가 큰 구간은 버티지만, 파지 직전 수 프레임의 정밀 정렬에서 먼저 무너진다 — 관찰된 실패 분포와 일치.

### 다음 라운드 조치
1. **카메라 스트림 압축 전송** — MJPEG 강제(`fourcc: MJPG`)로 USB 대역폭 문제를 해소하고 실시간성 확보 (→ [setup.md](../setup.md)).
2. **카메라 화면을 보며 시연** — 시연자가 정책과 동일한 관측(`--display_data=true`의 top/wrist 화면)만 보고 조작하도록 변경. 가설 (a) 대응.
3. **체계적 촬영 프로토콜 도입** — 모든 에피소드를 다음 순서로 통일:
   1. 전체뷰(`cam_top`)로 큐브 확인
   2. 손목캠(`cam_wrist`)으로 큐브 확인 후 파지
   3. 전체뷰로 컵 확인
   4. 컵 위로 이동
   5. 놓기
   6. 원위치 복귀
4. **정지 데이터 비율 40% → 20%** 로 축소 — 정지 행동은 이미 충분히 학습되었고, 성공 시연 표본을 늘리는 쪽이 이득.

---

## 3차 — 촬영 프로토콜 정비 후 재수집

### 데이터 구성
- 데이터셋: [`k-chan-l/pick_and_place3`](https://huggingface.co/datasets/k-chan-l/pick_and_place3)
- **50 에피소드 / 27,147 프레임 (평균 약 18.1초/에피소드)** — 그중 **정지 데이터 10개 (20%)**, 성공 시연 40개.
- 2차 대비 프레임 수가 약 1.9배로 늘었습니다. 정지 에피소드가 절반으로 줄고, 6단계 촬영 프로토콜을 지키면서 에피소드당 시연 길이 자체가 길어진 결과입니다.

### 촬영 방식
- 2차 조치 전부 반영:
  - 카메라 MJPEG 압축 스트림 (`fourcc: MJPG`, 640×480 / 30fps)
  - 시연자가 카메라 화면만 보며 조작
  - 6단계 촬영 프로토콜 준수 (전체뷰 큐브 확인 → 손목캠 확인 후 파지 → 전체뷰 컵 확인 → 컵 위로 이동 → 놓기 → 원위치)
- 에피소드 길이 30초, 리셋 5초.

### 학습
- 정책: [`k-chan-l/pick_and_place3_act`](https://huggingface.co/k-chan-l/pick_and_place3_act)
- ACT / ResNet-18 / chunk_size 100 / **batch_size 16** (2차와 동일) / 100,000 steps / save_freq 10,000 / num_workers 8
- **`image_transforms.enable=true` — 이미지 증강을 처음 켰습니다** (1·2차는 모두 `false`). ColorJitter(밝기·대비·채도·색조), SharpnessJitter, RandomAffine(±5°, ±5% 이동)이 적용됩니다.
- 학습 환경: **SSH로 원격 접속한 RTX 5070 Ti**. 로봇 제어 PC는 데이터 수집과 롤아웃만 담당합니다 (→ [setup.md](../setup.md) 4절).
- 학습 시 `--policy.push_to_hub=false` 였으나, 이후 수동으로 HF에 push 되어 현재 정책은 허브에 있습니다.

> **해석 시 주의 — 이번 라운드는 변수가 두 개입니다.** batch_size를 2차와 같은 16으로 둔 덕분에 배치 크기는 통제됐지만, **촬영 프로토콜 개선**과 **이미지 증강 활성화**가 동시에 들어갔습니다. 성공률이 올라가도 둘 중 어느 쪽 기여인지 이 라운드만으로는 분리되지 않습니다.

### 결과

**20회 중 4회 성공 (20%)** — `n_action_steps=100`(학습 기본값) 설정에서 평가.

| 항목 | 값 |
| --- | --- |
| 총 시행 | 20 |
| 성공 | **4 (20%)** |
| 근접 오차 | 1 |
| 파지 실패 | 10 |
| 파지 후 실패 | 5 |
| 무반응 | 0 |

시행별 기록:

| 회차 | 분류 | 관찰 |
| --- | --- | --- |
| 1 | 파지 실패 | 제대로 잡지 못한 채 컵으로 이동 |
| 2 | 파지 후 실패 | 파지 성공, 컵을 감지하지 못함 |
| 3 | 파지 실패 | 파지 실패 + 컵을 넘어뜨림 |
| 4 | 파지 실패 | |
| 5 | 파지 실패 | |
| 6 | **성공** | 한 번에 파지, 한 번에 투입 |
| 7 | **성공** | 파지 성공, 큐브가 굴러 들어감 |
| 8 | **성공** | 한 번에 파지, 한 번에 투입 |
| 9 | 파지 후 실패 | 그립이 얕아 "안 잡음"으로 인식. 컵 위에서 떨어뜨려 컵이 넘어짐 |
| 10 | 파지 실패 | |
| 11 | 파지 실패 | 잡았으나 "안 잡음"으로 인식해 놓침. 큐브가 굴러가 재파지 불가 |
| 12 | 파지 후 실패 | 파지 성공, 컵을 넘어뜨림 |
| 13 | 근접 오차 | **큐브가 팔이 닿지 않는 위치** — 무효 시행에 가까움 (→ [protocol.md](protocol.md)) |
| 14 | 파지 실패 | **큐브가 틀어져 있어** 파지 실패 |
| 15 | 파지 실패 | |
| 16 | 파지 후 실패 | 파지 성공, 컵을 찾지 못함 |
| 17 | 파지 후 실패 | 파지 성공, 컵 위치를 정확히 찾지 못함 |
| 18 | 파지 실패 | |
| 19 | **성공** | 파지·투입 성공 (컵이 넘어질 뻔함) |
| 20 | 파지 실패 | |

- 성공 4회 중 7·19회차는 아슬아슬한 성공이라 **안정적 성공은 실질 2회**로 보는 편이 정직합니다.
- 성공 시행의 **홈 복귀 여부가 기록되지 않았습니다.** 성공 기준 4개 중 하나이므로 다음 라운드부터 함께 기록합니다.
- 2차와 성공률 숫자는 같지만(20%), 2차는 10회 축약 평가에 지점 고정도 없었으므로 직접 비교는 성립하지 않습니다.

### 실패 원인 가설

1. **큐브 각도.** 시연자 관찰 — 잡기 쉬운 각도면 성공률이 높고 **틀어져 있으면 이상하게 잡습니다.** 큐브를 가운데 두고 잡는 게 아니라 조금 보이면 그 자리에서 그리퍼를 닫습니다. 학습 데이터의 큐브 각도 다양성 부족으로 보입니다.
2. **파지 인식 실패.** 9·11회차의 "잡았는데 안 잡은 걸로 인식". SO-101 관측에는 힘·촉각 정보가 없고 관절 상태만 들어갑니다. 큐브가 틀어진 채 잡히면 그리퍼 개폐량이 시연 분포와 달라지고, 정책이 그 상태를 "파지 전"으로 읽습니다. 1번과 같은 뿌리입니다.
3. **컵 단계 회귀.** 2차 기록에는 "잡기만 하면 컵까지 정상"이라고 되어 있으나, 3차는 파지 성공 9회 중 **5회가 컵 단계에서 깨졌습니다** (컵 미감지 2, 컵 전도 2, 위치 부정확 1). 3차에서 처음 켠 `image_transforms`의 채도 지터(0.5~1.5배)가 색으로 잡는 컵 단서를 흔들었을 가능성이 있습니다. 다만 2차의 파지 성공 표본이 작아 회귀로 단정하긴 이릅니다.

### 다음 라운드 조치

1. **추론 설정 점검** — `n_action_steps=100`은 3.33초 open-loop. 재학습 없이 검증 가능 → 실험 **A/B**.
2. **복구 시연 보강** — 실패 분포(파지 10 : 파지 후 5 ≈ 2:1)에 맞춰 배분 → **4차**.
3. 도달 불가 지점 교체, 컵 리셋 표시 → [protocol.md](protocol.md) 반영.

---

## 4차 — DAgger 교정 데이터 fine-tune (회귀)

### 데이터 구성
- 데이터셋: `k-chan-l/pick_and_place4` — **67 에피소드 / 33,547 프레임**
  - 3차 `pick_and_place3` 50ep / 27,147 프레임
  - DAgger 교정 `rollout_pick_and_place3_dagger` **17ep / 6,400 프레임**
- 교정은 `--strategy.record_autonomous=false`(corrections-only)로 수집했습니다. `intervention` 플래그가 **6,400/6,400 전부 True**로, 정책 자율 실행 프레임은 섞이지 않았습니다.
- 병합 시 `intervention` 컬럼을 제거한 `_clean` 사본을 사용했습니다 (3차 데이터에 없는 컬럼이라 스키마를 맞춰야 함).

### 학습
- **3차 체크포인트 `pick_and_place3_act`에서 `--policy.path`로 이어 학습.** from scratch 아님.
- **40,000 steps** / batch_size 16 / **`image_transforms.enable=false`**
- 정책: `k-chan-l/pick_and_place4_act`

> 3차 가중치는 증강 **on**으로 학습된 것인데 4차 fine-tune은 **off**입니다. **데이터 추가와 증강 제거가 동시에** 들어갔습니다 → 4b로 분리.

### 결과

**10회 중 0회 성공 (0%).**

| 관찰 | 횟수 |
| --- | --- |
| 헛집은 뒤 **재파지 시도** (3차엔 없던 행동) | 7 / 10 |
| 허공 잡음 — 큐브와 무관한 위치에서 그리퍼 닫음 | 3 / 10 |
| 컵 단계 도달 | 0 / 10 |

시행별: ①재파지 성공 후 컵 위치 실패 ②재파지 실패 ③재파지 안 함 ④재파지 성공 후 시간 초과 ⑤재파지 실패 ⑥파지 실패 ⑦재파지 실패 ⑧⑨⑩허공 잡음

- **큐브 인식이 거의 되지 않습니다.** 허공 잡음은 1차의 실패 양상입니다.
- 동시에 **재파지라는 새 행동은 확실히 학습됐습니다.** 교정 데이터의 *동작 패턴*은 들어왔는데 *시각적 근거*를 잃었습니다.
- 컵 단계는 도달 자체를 못 해 3차의 컵 문제는 검증되지 않았습니다.

### 실패 원인 진단

데이터 처리 자체에는 문제가 없었습니다 — 자율 프레임 미혼입, 병합 수치 일치(50+17=67, 27,147+6,400=33,547), 이미지 RGB 평균 차 0.01~0.03, 정규화 통계 `count=33547`로 재계산됨. 원인은 **교정 데이터의 성질**입니다.

**(a) 상태 분포가 좁은 영역에 뭉쳐 있습니다.** 교정 에피소드는 전부 실패 상태에서 시작하므로 홈 포지션도 초기 접근 구간도 한 프레임도 없습니다. 팔이 이미 큐브 근처로 내려간 자세만 6,400프레임입니다.

| 관절 | 3차 | 병합 후 |
| --- | --- | --- |
| 관절2 최대 | 49.1 | 61.3 |
| 관절3 최소 | −54.0 | −80.4 |
| 관절3 **중앙값** | **−3.4** | **−57.6** |
| 관절5 최대 | 36.2 | 54.5 |

19% 프레임을 추가했는데 관절3 중앙값이 54도 이동했습니다.

**(b) 같은 관측에 서로 다른 액션이 붙어 있습니다.** 그 구간에서 화면은 거의 변하지 않는데(손목캠이 큐브를 계속 봄) 액션은 재시도·더듬기·각도 변경으로 제각각입니다. 행동복제 모델은 여기서 "이 관측에서 액션은 예측 불가"를 배우고 **평균 액션으로 수렴**합니다. 그것이 관측을 무시한 평균 궤적 재생 = 허공 잡음입니다. 하필 **큐브 바로 앞, 가장 중요한 시각 구간**을 이 노이즈가 덮었습니다.

(a)와 (b)는 재파지 행동의 출현과 파지 정밀도 회귀를 **하나의 원인으로** 묶어 설명합니다.

**경합 가설 — 증강 제거.** 3차는 증강 on으로 학습된 가중치인데 4차 fine-tune은 off입니다. 시각적 강건성이 40k steps 동안 좁은 데이터에 과적합되며 무너졌을 수 있습니다. 두 가설은 배타적이지 않으며, 4b가 이를 가릅니다.

### 다음 라운드 조치
- **4b** — 동일 조건 증강 on으로 원인 분리.
- 교정 17ep는 4b 결과가 나올 때까지 **보류**. 재수집 시에는 더듬는 과정 없이 한 번에 깔끔하게 복구되는 시연만.

---

## 4b — 4차와 동일 조건, 증강만 on

- 4차와 **데이터·초기 가중치·steps 전부 동일**하고 `image_transforms.enable=true` 만 다릅니다.
  - `pick_and_place4`(67ep) / 3차 체크포인트에서 40,000 steps / batch_size 16
- 목적: 4차 회귀가 **증강 제거** 탓인지 **DAgger 노이즈** 탓인지 분리.
- 평가는 `n_action_steps=100`으로 10회 (4차의 0/10과 같은 설정).

### 결과

**10회 중 0회 성공 (0%).** 성공률은 4차와 같지만 **거동이 완전히 다릅니다.**

| 항목 | 값 |
| --- | --- |
| 총 시행 | 10 |
| 성공 | 0 |
| 파지 실패 | 8 |
| 파지 후 실패 | 2 |
| 근접 오차 | 0 |
| 무반응 | 0 |
| **큐브 근접** | **10 / 10** |

시행별 기록 (시연자 직접 관찰):

| 회차 | 분류 | 관찰 |
| --- | --- | --- |
| 1 | 파지 실패 | 큐브를 잡았으나 이동 중 놓침 |
| 2 | 파지 실패 | 큐브 주변에서 계속 파지 시도, 전부 실패 |
| 3 | 파지 실패 | 파지 실패 후 재파지도 실패 |
| 4 | 파지 실패 | |
| 5 | 파지 실패 | |
| 6 | 파지 실패 | |
| 7 | 파지 실패 | |
| 8 | 파지 실패 | |
| 9 | 파지 후 실패 | 파지 성공, 컵 위치를 찾지 못함 |
| 10 | 파지 후 실패 | 파지 성공, 컵 위치를 찾지 못함 |

요약: 정밀 파지 실패 7 / 파지 중 놓침 1 / 파지 성공 2 (둘 다 컵 단계에서 실패).

**핵심 관찰** — 손목캠에 큐브가 보이지만 **중앙 정렬이 되지 않은 상태에서 파지 모션을 실행**합니다. 큐브 근처까지는 정확히 가는데, 마지막 정렬 없이 그 자리에서 그리퍼를 닫습니다.

### 라운드 비교

| | 3차 | 4차 | 4b |
| --- | --- | --- | --- |
| 데이터 | 3차 50ep | 3차 + DAgger 17ep | 3차 + DAgger 17ep |
| 증강 | on | **off** | **on** |
| 초기화 | from scratch 100k | 3차에서 40k | 3차에서 40k |
| 성공 | **4/20 (20%)** | 0/10 | 0/10 |
| 큐브 근접 | 19/20 (95%) | 허공 잡음 3/10 | **10/10 (100%)** |
| 파지 성공 | **9/20 (45%)** | 0/10 | 2/10 (20%) |
| 컵 단계 도달 | 9 | 0 | 2 |

### 결론

**1. 4차 허공 잡음의 주범은 증강 제거입니다.** 데이터·초기 가중치·steps가 전부 같은데 증강만 켜자 큐브 근접이 3/10 수준에서 **10/10으로 복구**됐습니다. 증강이 시각적 주의를 받치고 있었고, 40k steps 동안 증강 없이 좁은 데이터에 학습하자 그것이 무너진 것입니다.
→ **5차 증강 on 확정.**

**2. DAgger 17ep는 정밀 파지에 기여가 없거나 유해합니다.** 4b는 근접을 되찾았는데도 파지 성공률이 **3차 45% → 4b 20%** 로 오히려 낮습니다. 증강이라는 교란 변수를 제거하고 보면 교정 데이터가 정밀도를 끌어올리지 못했습니다.
→ **5차에서 17ep 제외 확정.**

> 4차 시점의 진단(교정 데이터가 좁은 관절 영역에 뭉쳐 평균화)은 **허공 잡음의 원인으로는 반증**됐습니다. 그 부분은 증강 제거가 설명합니다. 다만 교정 데이터가 정밀 파지를 개선하지 못한 것은 그대로 관측됐으므로, 진단의 방향 자체가 틀린 것은 아닙니다.

**3. 1~4차를 관통하는 근본 문제가 드러났습니다.** 정책은 **"큐브가 보이면 잡는다"는 배웠지만 "큐브를 중앙에 정렬한 뒤 잡는다"는 배우지 못했습니다.**

- 3차 시연자 관찰: "큐브를 가운데 두고 잡지 않고 조금 보이면 잡는다"
- 4b 시연자 관찰: "손목캠에 큐브가 보이지만 중앙에 있지 않은데도 파지 모션 실행"
- A/B 실험: 폐루프로 바꾸자 오히려 나빠짐 — 관측을 정렬 신호로 쓰지 못한다는 증거

세 라운드의 관찰이 같은 결론을 가리킵니다. 시연 데이터에 **정렬 과정이 관측 가능한 형태로 남아 있지 않은 것**이 원인으로 보입니다. 사람은 정렬을 순간적으로 끝내므로, 30fps에서 정렬 구간이 몇 프레임에 불과해 학습 신호가 되지 못합니다.
→ **5차 시연 프로토콜에 정렬 구간 명시적 기록 추가** (→ [protocol.md](protocol.md)).

---

## 실험 A / B — 재학습 없이 폐루프 개선 시도 (실패)

`n_action_steps=100`은 100스텝치 액션을 한 번에 뽑아 끝까지 실행한 뒤에야 다시 관측한다는 뜻입니다 (30fps 기준 **3.33초 open-loop**). 3차의 "큐브를 가운데 두고 잡지 않고 조금 보이면 닫는다"가 이 증상과 맞아, **재학습 없이** 추론 설정만 바꿔 검증했습니다. 대상은 3차 정책 `pick_and_place3_act`.

| 실험 | 설정 | 결과 |
| --- | --- | --- |
| 기준 | `n_action_steps=100` | **4/20 (20%)** |
| **A** | `n_action_steps=20` | **0/3** — 큐브 발견 후 진동하다 정지. 재계획마다 예측 불일치. 3회로 중단 |
| **B** | `temporal_ensemble_coeff=0.01`, `n_action_steps=1` | **0/10** — 움직임은 느리고 부드러움. 그리퍼 개폐가 무뎌짐(평균화 부작용). 정상 파지 2/10, 헛잡기 반복 6/10, 큐브 위 정지 2/10 |

**결론: 100 > TE > 20.** 관측을 자주 넣을수록 나빠졌습니다. **가설이 반증됐습니다.**

이건 단순한 음성 결과가 아니라 **진단**입니다. 폐루프로 바꿨을 때 성능이 떨어진다는 건, 정책이 관측을 보고 목표에 맞춰 궤적을 수정하는 것이 아니라 **관측을 거의 쓰지 못한 채 초기에 커밋한 긴 궤적으로 버티고 있다**는 뜻입니다. 재계획을 자주 시키면 매번 다른 예측이 나와 진동하고(A), 평균을 내면 그리퍼처럼 이산적인 동작이 뭉개집니다(B).

즉 병목은 추론 설정이 아니라 **정책이 관측을 쓰도록 학습되지 않았다는 것**이며, 이는 데이터 문제입니다.

→ **추론 설정으로 해결 불가. 데이터 재수집(5차) 결정.**

---

# 통제 실험 (5차~)

## 5차 (촬영 중) — 통제 재시작

1~4차에서 얻은 결론을 전부 반영해 **데이터를 새로 수집하고 from scratch로 학습**합니다. 이전 라운드의 데이터·가중치를 이어받지 않습니다.

촬영 조건, 큐브 지점 배치, 시연 프로토콜, 평가 방법은 **[protocol.md](protocol.md)의 "5차 통제 실험 조건"** 에 정의되어 있습니다. 이 문서에는 결과만 기록합니다.

핵심 설계 근거:

| 조치 | 근거 |
| --- | --- |
| from scratch, 100k steps | 4차에서 fine-tune이 좁은 데이터에 끌려간 것을 차단 |
| 큐브 7지점 × 6회, 각도 0°/45° 절반씩 | 3차 가설 1 — 각도 다양성 부족 |
| **정렬 구간을 1~2초 천천히** | 4b 결론 3 — 중앙 정렬을 학습하지 못함. 정렬 과정을 학습 신호가 될 길이로 남김 |
| DAgger 교정 17ep 제외 | 4b 결론 2 — 정밀 파지에 기여 없음 |
| 증강 on | 4b 결론 1 — 증강이 시각적 주의를 받침 |
| 파지 후 멈춤 없이 곧장 컵 | 3차 가설 3 — 컵 단계 실패. 멈칫한 에피소드는 즉시 삭제 |
| 컵 위치 고정 + 바닥 표시 | 3차에서 컵 전도 3회, 시행 조건이 흔들림 |
| 카메라 자동 노출·화이트밸런스 off | 프레임 간 밝기·색 흔들림 제거 (컵은 색 단서 의존) |
| 배치 템플릿을 촬영 전 제거 | 지점 표시가 프레임에 남으면 정책이 그것을 단서로 학습 |
| 정상 42 + 정지 8 (총 50) | 정지 행동은 2차에서 이미 학습 확인됨. 비율 20%→16%로 축소 |

- 결과: **미기록**
