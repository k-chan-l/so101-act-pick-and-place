# 실험 A / B — 재학습 없이 폐루프 개선 시도

> [← 4b](round4b.md) · [전체 목차](../experiments.md) · [5차 →](round5.md)

| | |
| --- | --- |
| 단계 | 탐색 |
| 검증하려는 가설 | 추론을 폐루프화하면 파지 정밀도가 오른다 |
| **판정** | **반증** |
| 근거 | 100 > TE > 20. 폐루프일수록 악화 |

---
## 설계

`n_action_steps=100`은 100스텝치 액션을 한 번에 뽑아 끝까지 실행한 뒤에야 다시 관측한다는 뜻입니다 (30fps 기준 **3.33초 open-loop**). 3차의 "큐브를 가운데 두고 잡지 않고 조금 보이면 닫는다"가 이 증상과 맞아, **재학습 없이** 추론 설정만 바꿔 검증했습니다. 대상은 3차 정책 `pick_and_place3_act`.

## 결과

| 실험 | 설정 | 결과 |
| --- | --- | --- |
| 기준 | `n_action_steps=100` | **4/20 (20%)** |
| **A** | `n_action_steps=20` | **0/3** — 큐브 발견 후 진동하다 정지. 재계획마다 예측 불일치. 3회로 중단 |
| **B** | `temporal_ensemble_coeff=0.01`, `n_action_steps=1` | **0/10** — 움직임은 느리고 부드러움. 그리퍼 개폐가 무뎌짐(평균화 부작용). 정상 파지 2/10, 헛잡기 반복 6/10, 큐브 위 정지 2/10 |

A·B는 실패 유형을 분류하지 않았습니다 — 추론 설정 비교가 목적이라 성공률과 거동 관찰만 남겼습니다. B의 집계는 정상 파지 2 / 헛잡기 반복 6 / 큐브 위 정지 2 입니다.

## 실패 분석

**결론: 100 > TE > 20.** 관측을 자주 넣을수록 나빠졌습니다. **가설이 반증됐습니다.**

이건 단순한 음성 결과가 아니라 **진단**입니다. 폐루프로 바꿨을 때 성능이 떨어진다는 건, 정책이 관측을 보고 목표에 맞춰 궤적을 수정하는 것이 아니라 **관측을 거의 쓰지 못한 채 초기에 커밋한 긴 궤적으로 버티고 있다**는 뜻입니다. 재계획을 자주 시키면 매번 다른 예측이 나와 진동하고(A), 평균을 내면 그리퍼처럼 이산적인 동작이 뭉개집니다(B).

즉 병목은 추론 설정이 아니라 **정책이 관측을 쓰도록 학습되지 않았다는 것**이며, 이는 데이터 문제입니다.

→ **추론 설정으로 해결 불가. 데이터 재수집(5차) 결정.**

---

---

## 다음 라운드 결정 근거

추론 설정만으로는 해결되지 않는다는 것이 확인됐으므로, **데이터를 다시 모으기로** 결정했습니다 → [5차](round5.md).

이 음성 결과는 5차 이후에도 쓰였습니다. 5차의 "겹친 지점 + open-loop commitment" 해석에서, 폐루프화가 오히려 악화됐다는 사실이 **프레임 단위 시각 신호가 신뢰할 수 없다**는 방증으로 인용됩니다 → [5차 추론 ④](round5.md)

또한 이후 모든 평가의 기준선을 `n_action_steps=100`으로 고정하는 근거가 됐습니다 → [protocol.md](../protocol.md)

