# 3차 — 촬영 프로토콜 정비 후 재수집

> [← 2차](round2.md) · [전체 목차](../experiments.md) · [4차 →](round4.md)

| | |
| --- | --- |
| 단계 | 탐색 |
| 검증하려는 가설 | 시연자 특권 정보와 프레임 드랍이 정밀 파지를 막는다 |
| **판정** | **확인** |
| 근거 | MJPEG + 카메라 화면 시연으로 4/20, 최고 성적 갱신 |

---
### 데이터 구성
- 데이터셋: [`k-chan-l/pick_and_place3`](https://huggingface.co/datasets/k-chan-l/pick_and_place3)
- **50 에피소드 / 27,147 프레임 (평균 약 18.1초/에피소드)** — 그중 **정지 데이터 10개 (20%)**, 성공 시연 40개.
- 2차 대비 프레임 수가 약 1.9배로 늘었습니다. 정지 에피소드가 절반으로 줄고, 6단계 촬영 프로토콜을 지키면서 에피소드당 시연 길이 자체가 길어진 결과입니다.

### 촬영 방식
- 2차 조치 전부 반영:
  - 카메라 MJPEG 압축 스트림 (`fourcc: MJPG`, 640×480 / 30fps)
  - 시연자가 카메라 화면만 보며 조작
  - 6단계 촬영 프로토콜 준수 (전체뷰 큐브 확인 → 손목캠 확인 후 파지 → 전체뷰 컵 확인 → 컵 위로 이동 → 놓기 → 원위치)
- 에피소드 길이 30초, 리셋 5초.

### 학습
- 정책: [`k-chan-l/pick_and_place3_act`](https://huggingface.co/k-chan-l/pick_and_place3_act)
- ACT / ResNet-18 / chunk_size 100 / **batch_size 16** (2차와 동일) / 100,000 steps / save_freq 10,000 / num_workers 8
- **`image_transforms.enable=true` — 이미지 증강을 처음 켰습니다** (1·2차는 모두 `false`). ColorJitter(밝기·대비·채도·색조), SharpnessJitter, RandomAffine(±5°, ±5% 이동)이 적용됩니다.
- 학습 환경: **SSH로 원격 접속한 RTX 5070 Ti**. 로봇 제어 PC는 데이터 수집과 롤아웃만 담당합니다 (→ [setup.md](../../setup.md) 4절).
- 학습 시 `--policy.push_to_hub=false` 였으나, 이후 수동으로 HF에 push 되어 현재 정책은 허브에 있습니다.

> **해석 시 주의 — 이번 라운드는 변수가 두 개입니다.** batch_size를 2차와 같은 16으로 둔 덕분에 배치 크기는 통제됐지만, **촬영 프로토콜 개선**과 **이미지 증강 활성화**가 동시에 들어갔습니다. 성공률이 올라가도 둘 중 어느 쪽 기여인지 이 라운드만으로는 분리되지 않습니다.

### 결과

**20회 중 4회 성공 (20%)** — `n_action_steps=100`(학습 기본값) 설정에서 평가.

| 항목 | 값 |
| --- | --- |
| 총 시행 | 20 |
| 성공 | **4 (20%)** |
| 근접 오차 | 1 |
| 파지 실패 | 10 |
| 파지 후 실패 | 5 |
| 무반응 | 0 |

시행별 기록:

| 회차 | 분류 | 관찰 |
| --- | --- | --- |
| 1 | 파지 실패 | 제대로 잡지 못한 채 컵으로 이동 |
| 2 | 파지 후 실패 | 파지 성공, 컵을 감지하지 못함 |
| 3 | 파지 실패 | 파지 실패 + 컵을 넘어뜨림 |
| 4 | 파지 실패 | |
| 5 | 파지 실패 | |
| 6 | **성공** | 한 번에 파지, 한 번에 투입 |
| 7 | **성공** | 파지 성공, 큐브가 굴러 들어감 |
| 8 | **성공** | 한 번에 파지, 한 번에 투입 |
| 9 | 파지 후 실패 | 그립이 얕아 "안 잡음"으로 인식. 컵 위에서 떨어뜨려 컵이 넘어짐 |
| 10 | 파지 실패 | |
| 11 | 파지 실패 | 잡았으나 "안 잡음"으로 인식해 놓침. 큐브가 굴러가 재파지 불가 |
| 12 | 파지 후 실패 | 파지 성공, 컵을 넘어뜨림 |
| 13 | 근접 오차 | **큐브가 팔이 닿지 않는 위치** — 무효 시행에 가까움 (→ [protocol.md](../protocol.md)) |
| 14 | 파지 실패 | **큐브가 틀어져 있어** 파지 실패 |
| 15 | 파지 실패 | |
| 16 | 파지 후 실패 | 파지 성공, 컵을 찾지 못함 |
| 17 | 파지 후 실패 | 파지 성공, 컵 위치를 정확히 찾지 못함 |
| 18 | 파지 실패 | |
| 19 | **성공** | 파지·투입 성공 (컵이 넘어질 뻔함) |
| 20 | 파지 실패 | |

- 성공 4회 중 7·19회차는 아슬아슬한 성공이라 **안정적 성공은 실질 2회**로 보는 편이 정직합니다.
- 성공 시행의 **홈 복귀 여부가 기록되지 않았습니다.** 성공 기준 4개 중 하나이므로 다음 라운드부터 함께 기록합니다.
- 2차와 성공률 숫자는 같지만(20%), 2차는 10회 축약 평가에 지점 고정도 없었으므로 직접 비교는 성립하지 않습니다.

### 실패 원인 가설

1. **큐브 각도.** 시연자 관찰 — 잡기 쉬운 각도면 성공률이 높고 **틀어져 있으면 이상하게 잡습니다.** 큐브를 가운데 두고 잡는 게 아니라 조금 보이면 그 자리에서 그리퍼를 닫습니다. 학습 데이터의 큐브 각도 다양성 부족으로 보입니다.
2. **파지 인식 실패.** 9·11회차의 "잡았는데 안 잡은 걸로 인식". SO-101 관측에는 힘·촉각 정보가 없고 관절 상태만 들어갑니다. 큐브가 틀어진 채 잡히면 그리퍼 개폐량이 시연 분포와 달라지고, 정책이 그 상태를 "파지 전"으로 읽습니다. 1번과 같은 뿌리입니다.
3. **컵 단계 회귀.** 2차 기록에는 "잡기만 하면 컵까지 정상"이라고 되어 있으나, 3차는 파지 성공 9회 중 **5회가 컵 단계에서 깨졌습니다** (컵 미감지 2, 컵 전도 2, 위치 부정확 1). 3차에서 처음 켠 `image_transforms`의 채도 지터(0.5~1.5배)가 색으로 잡는 컵 단서를 흔들었을 가능성이 있습니다. 다만 2차의 파지 성공 표본이 작아 회귀로 단정하긴 이릅니다.

### 다음 라운드 조치

1. **추론 설정 점검** — `n_action_steps=100`은 3.33초 open-loop. 재학습 없이 검증 가능 → 실험 **A/B**.
2. **복구 시연 보강** — 실패 분포(파지 10 : 파지 후 5 ≈ 2:1)에 맞춰 배분 → **4차**.
3. 도달 불가 지점 교체, 컵 리셋 표시 → [protocol.md](../protocol.md) 반영.

---
